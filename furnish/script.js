const menuToggle=document.getElementById("menuToggle");const mobileMenu=document.getElementById("mobileMenu");if(menuToggle&&mobileMenu){menuToggle.addEventListener("click",()=>{mobileMenu.style.display=mobileMenu.style.display==="flex"?"none":"flex"})}const slides=document.querySelectorAll(".slide");const prevBtn=document.getElementById("prev");const nextBtn=document.getElementById("next");if(slides.length&&prevBtn&&nextBtn){let currentIndex=0;function showSlide(index){slides.forEach(slide=>slide.classList.remove("active"));slides[index].classList.add("active")}function nextSlide(){currentIndex++;if(currentIndex>=slides.length)currentIndex=0;showSlide(currentIndex)}function prevSlide(){currentIndex--;if(currentIndex<0)currentIndex=slides.length-1;showSlide(currentIndex)}nextBtn.addEventListener("click",nextSlide);prevBtn.addEventListener("click",prevSlide);setInterval(nextSlide,4000)}const loadMoreBtn=document.getElementById("loadMoreBtn");const extraCards=document.querySelectorAll(".hidden-extra");if(loadMoreBtn){loadMoreBtn.addEventListener("click",()=>{extraCards.forEach(card=>{card.style.display="block"});loadMoreBtn.style.display="none"})}const contactForm=document.getElementById("contactForm");if(contactForm){contactForm.addEventListener("submit",e=>{e.preventDefault();alert("Your message has been sent successfully.");contactForm.reset()})}const applyFiltersBtn=document.getElementById("applyFilters");const productsGrid=document.getElementById("productsGrid");const sortSelect=document.getElementById("sortSelect");function getSelectedCategories(){return[...document.querySelectorAll(".category-filter:checked")].map(item=>item.value)}function getSelectedPrice(){const selected=document.querySelector(".price-filter:checked");return selected?selected.value:null}function priceMatches(price,range){if(!range)return true;if(range==="under100")return price<100;if(range==="100to300")return price>=100&&price<=300;if(range==="above300")return price>300;return true}function filterProducts(){if(!productsGrid)return;const selectedCategories=getSelectedCategories();const selectedPrice=getSelectedPrice();const products=document.querySelectorAll(".product-card");products.forEach(product=>{const category=product.dataset.category;const price=parseFloat(product.dataset.price);const categoryOk=selectedCategories.length===0||selectedCategories.includes(category);const priceOk=priceMatches(price,selectedPrice);product.style.display=categoryOk&&priceOk?"block":"none"})}function sortProducts(){if(!productsGrid||!sortSelect)return;const cards=[...document.querySelectorAll(".product-card")];const sortValue=sortSelect.value;cards.sort((a,b)=>{const priceA=parseFloat(a.dataset.price);const priceB=parseFloat(b.dataset.price);if(sortValue==="low")return priceA-priceB;if(sortValue==="high")return priceB-priceA;return 0});cards.forEach(card=>productsGrid.appendChild(card))}if(applyFiltersBtn){applyFiltersBtn.addEventListener("click",filterProducts)}if(sortSelect){sortSelect.addEventListener("change",()=>{sortProducts();filterProducts()})}